# Git Push Workflow

Always push to **both** remotes when pushing changes:

```bash
git push atc main && git push origin main
```

- `atc` → git@atc-github.azure.cloud.bmw:q475567/diagram-docs.git
- `origin` → https://github.com/feigi/diagram-docs.git
